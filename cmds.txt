python3 classifier.py –fine-tune-mode last-linear-layer –batch_size BATCH_SIZE –lr LR hidden_dropout_prob=RATE –epochs=NUM_EPOCHS

full-model

python3 -c "import torch; print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'No CUDA GPU detected')"

  parser.add_argument("--seed", type=int, default=11711)
  parser.add_argument("--epochs", type=int, default=10)
  parser.add_argument("--fine-tune-mode", type=str,
                      help='last-linear-layer: the GPT parameters are frozen and the task specific head parameters are updated; full-model: GPT parameters are updated as well',
                      choices=('last-linear-layer', 'full-model'), default="last-linear-layer")
  parser.add_argument("--use_gpu", action='store_true')

  parser.add_argument("--batch_size", help='sst: 64, cfimdb: 8 can fit a 12GB GPU', type=int, default=8)
  parser.add_argument("--hidden_dropout_prob", type=float, default=0.3)
  parser.add_argument("--lr", type=float, help="learning rate, default lr for 'pretrain': 1e-3, 'finetune': 1e-5",
                      default=1e-3)
        

python3 classifier.py \
  --use_gpu \
  --fine-tune-mode full-model \
  --lr 1e-5

python3 classifier.py \
  --use_gpu \
  --fine-tune-mode full-model \
  --batch_size 16 \
  --hidden_dropout_prob 0.1 \
  --epochs 10


python3 paraphrase_detection.py --use_gpu

python sonnet_generation.py --use_gpu

python -c "from evaluation import test_sonnet; print(test_sonnet())"